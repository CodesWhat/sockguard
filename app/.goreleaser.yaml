version: 2

project_name: sockguard

before:
  hooks:
    - go mod tidy

builds:
  - id: sockguard
    main: ./cmd/sockguard/
    binary: sockguard
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/codeswhat/sockguard/app/internal/version.Version={{.Version}}
      - -X github.com/codeswhat/sockguard/app/internal/version.Commit={{.Commit}}
      - -X github.com/codeswhat/sockguard/app/internal/version.BuildDate={{.Date}}
    flags:
      - -trimpath

archives:
  - id: default
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

sboms:
  - id: cyclonedx
    documents:
      - "${artifact}.cyclonedx.json"
    cmd: syft
    # Mirrors GoReleaser's own default syft args (sbom.go:80) apart from the
    # output format. Naming `args:` at all replaces the whole default slice, so
    # --enrich all has to be restated or the SBOM ships without dependency
    # license data.
    args: ["$artifact", "--output", "cyclonedx-json=$document", "--enrich", "all"]
    artifacts: archive

# Keyless cosign signatures for the per-platform archives users actually
# download. Until now only `sockguard-<tag>.tar.gz` -- the `git archive`
# SOURCE snapshot built in the workflow's separate `release` job -- carried a
# .sig/.pem, and the similar filename made that easy to mistake for coverage
# of the binaries. It wasn't: v1.7.1 shipped four
# `sockguard_1.7.1_<os>_<arch>.tar.gz` archives with a CycloneDX SBOM and
# nothing else.
#
# Signs checksums.txt as well as the archives, so a consumer can verify the
# whole release from one signature or any single archive on its own.
#
# Keyless needs `id-token: write` on the goreleaser job and the cosign binary
# on PATH -- both added alongside this in release-from-tag.yml.
signs:
  - id: cosign-keyless
    cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--yes"
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
    artifacts: checksum
  - id: cosign-keyless-archives
    cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--yes"
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
    artifacts: archive

snapshot:
  version_template: "{{ incpatch .Version }}-dev"

changelog:
  disable: true  # Release notes come from CHANGELOG.md via extract-changelog-entry.mjs

release:
  draft: false
  prerelease: auto  # RC tags marked as prerelease

homebrew_casks:
  - name: sockguard
    ids:
      - default
    binaries:
      - sockguard
    homepage: https://getsockguard.com/
    description: Default-deny Docker socket proxy with request-aware policy enforcement
    caveats: |
      Sockguard needs access to a Docker socket and an explicit policy configuration.
      See https://getsockguard.com/docs/getting-started before starting it.
      The macOS binary is not yet Apple notarized. This cask removes its quarantine
      attribute after Homebrew verifies the release archive checksum.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/sockguard"]
          end
    skip_upload: auto
    repository:
      owner: CodesWhat
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: security@codeswhat.com
